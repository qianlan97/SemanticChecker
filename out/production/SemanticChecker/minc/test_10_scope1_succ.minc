
func main::()->int
begin
    var a::int;
    var b::int;
    var ga::int;
    var gb::int;
    var gc ::bool;
    begin
        var la::int;
        var lb::int ;
        begin
            var a::bool;
            var b::bool;
            var ga::bool ;
            ga <- true;
            a <- true;
            b <- true;
        end
        a <- 1;
        b <- 20;
        ga <- 3;
        gb <- 40;
        gc <- true;
        begin
            var la::bool;
            la <- a < ga and b >= gb;
            gc <- gc and la;
        end
        print gc;
    end
    return gb;
end
