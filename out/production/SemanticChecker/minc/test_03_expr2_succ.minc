func main::()->int
begin
    var x::int;
    var y::int;
    var z::bool;
    x <- 0;
    x <- x + 1;
    y <- 10 * 21 + 35 / (41 - 53);
    z <- not true and 1 < 3 and y >= 42 or true = (x < 2);
    print z;
    return y;
end
